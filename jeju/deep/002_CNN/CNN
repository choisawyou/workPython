CNN(Convolution Neural Network, 적분) : 
    원본 이미지에 필터(3,3정도) 적용, 
    (3,3에서 특징추출, 곱의 합)하나의 값이 나온다. 
    한칸씩옮겨 가면서 값을 하나씩 추출, 여러개의 필터를 사용한다. channel증가, 필터도 학습한다.
    특징을 추출하여 flatten(일렬)하여 FFNN을하고 가중치를 곱한다?
    
    FFNN을 하기위해서 CNN을한다. 테두리에 중요한 특징이 있을수 있다. padding으로 채워서 추출
    
    pooling으로 사이즈를 줄여준다. 중복 추출된 특징을 줄이기 위해서,
    
    가로, 세로, RGB 3channel, 형태가 중요할때 흑백으로 한다. 필터가 여러개가 적용되어 channel증가
    
    stride : 건너띄기, 2,3칸씩 건너띄면서 특징추출